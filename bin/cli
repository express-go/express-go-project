#!/usr/bin/env node
/**
 * Command-line interface
 */
var debug   = require('debug')('express-go:server');
var app     = require('../app.js');

require('express-go/cli')( app, __dirname + '/../' );
